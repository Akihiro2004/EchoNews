// TEMPORARY DEVELOPMENT RULES - USE ONLY FOR TESTING
// Replace with proper rules from FIREBASE_RULES.md for production

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated users to read and write everything during development
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
